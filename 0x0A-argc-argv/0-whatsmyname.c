#include "main.h"

/**
 * main - main program
 * @argc: number of arguments pass to the program
 * @argv: string pointers to an array of arguments passed
 *
 * Return: 0
 */
int main(int argc, char *argv[])
{
	UNUSED(argc);
	printf("%s\n", argv[0]);
	return (0);
}
