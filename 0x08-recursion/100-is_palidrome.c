#include <stdio.h>
#include "main.h"

/**
 * is_palindrome - function to check is a string is a palindrome
 * @s: pointer to the string to evaluate
 *
 * Return: 1 if true else 0
 */
int is_palindrome(char *s)
{

}
int len = 0;
int str_rev(char *s)
{
	char *str;

	if (_strlen_recursion(s) == 1)
		return *str = *s;
	else
	{
	(str) = *(s + _strlen_recursion


